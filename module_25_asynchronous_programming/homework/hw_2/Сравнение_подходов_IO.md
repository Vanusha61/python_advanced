| Количество запросов | Потоки (Threads), с | Процессы (Processes), с | Асинхронность (asyncio), с |
|--------------------|--------------------|-------------------------|----------------------------|
| 10                 | 1.140              | 1.310                   | 0.216                      |
| 50                 | 5.301              | 5.507                   | 0.242                      |
| 100                | 10.502             | 10.762                  | 0.292                      |

Выводы:

Асинхронный подход на базе asyncio + aiohttp показывает наилучшее время выполнения при любом количестве запросов (10, 50, 100). Это объясняется тем, что event loop позволяет эффективно мультиплексировать сетевые операции в одном потоке, не тратя ресурсы на создание и переключение потоков/процессов.
Потоки и процессы показывают близкие результаты, причём потоки чуть быстрее процессов, так как создание процесса тяжелее. Оба подхода уступают асинхронности из-за накладных расходов на управление потоками/процессами и блокирующих вызовов requests.get().
С ростом числа запросов (от 10 до 100) время выполнения потоков и процессов растёт почти линейно, тогда как у асинхронности рост незначителен (всего +0.076 с при увеличении нагрузки в 10 раз). Это говорит о хорошей масштабируемости асинхронного подхода для I/O-задач.
Запись в файл выполнялась через asyncio.to_thread, что не создаёт дополнительных задержек, и её время не влияет на общую картину (сравнимо с aiofiles